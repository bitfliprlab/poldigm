# 📄 22_에셋_및_캐릭터_명세서

## 1. 문서 개요

본 문서는 폴다임(Poldigm)의 결과 화면, 공유 이미지, 랜딩 비주얼에 사용될 **MVP 에셋 계약**을 정의합니다. 현재 로컬 MVP는 실제 캐릭터 PNG 없이 타입 코드, 결과명, 게이지, 텍스트 중심으로 결과를 렌더링합니다. 캐릭터 일러스트는 실제 MVP 이후 확장 가능한 선택 에셋으로 관리합니다.

## 2. 에셋 원칙

- **모바일 우선:** 모든 결과 화면과 공유 이미지는 모바일 결과 화면에서 먼저 검증합니다.
- **타입 코드 기반:** 캐릭터 파일명은 결과 코드의 4자리 기본 유형(`CTMO`, `IPEL` 등)을 기준으로 관리합니다.
- **S/M 태그 분리:** 강도 태그는 동일 캐릭터 위에 배지, 컬러, 텍스트 톤으로 표현하며 캐릭터 파일을 `-S`, `-M`으로 중복 제작하지 않습니다.
- **구현 우선 대체 가능:** 최종 일러스트가 없을 때는 타입 코드, 결과명, 게이지 중심 UI만으로 결과 화면을 완성합니다. 캐릭터 placeholder는 컴포넌트로 보존할 수 있으나 현재 기본 결과 카드에는 표시하지 않습니다.

## 3. 캐릭터 일러스트 파일 규칙

아래 규칙은 실제 캐릭터 에셋을 도입할 때의 파일명 계약입니다. 현재 로컬 MVP에서는 실제 PNG가 없어도 완료 조건을 충족합니다.

| 항목 | 규격 |
|---|---|
| 저장 위치 | `static/assets/characters/` |
| 파일명 | `character_{type_code}.png` |
| 예시 | `character_CTMO.png`, `character_IPEL.png` |
| 권장 크기 | 1024 x 1024 px |
| 표시 비율 | 1:1 정사각형 |
| 배경 | 투명 배경 PNG 권장 |
| 최소 표시 크기 | 캐릭터를 다시 도입할 경우 모바일 결과 화면 기준 가로 160~220px |
| 대체 텍스트 | `{title} 캐릭터 일러스트` |

## 4. 캐릭터 아트 스타일

결과 일러스트는 사용자의 정치 성향을 단정하는 이미지가 아니라, 결과 유형을 SNS에서 공유하고 싶게 만드는 보조 시각 요소로만 사용합니다. 실제 정치인, 정당, 선거 캠페인처럼 보이지 않도록 상징과 색상 사용을 절제합니다.

### 4.1. 공통 스타일 방향

| 항목 | 기준 |
|---|---|
| 스타일명 | Minimal Persona Mark |
| 표현 방식 | 세미 플랫 벡터 또는 단순한 타입 코드 기반 마크 |
| 분위기 | 블랙/그레이/화이트 중심의 앱 UI에 어울리는 절제된 톤 |
| 형태 | 결과 카드의 핵심 정보를 방해하지 않는 작은 보조 요소 |
| 선/면 처리 | 또렷한 실루엣, 단순한 면 분할, 과도한 질감과 세밀 묘사는 지양 |
| 색감 | 모노톤 중심, 필요한 경우 코랄 포인트 1색만 사용 |
| 배경 | 투명 PNG 기본. 배경 효과가 필요하면 UI에서 별도 처리 |
| 표정 | 공격적이거나 조롱하는 표정보다 자신감, 사려 깊음, 몰입감 중심 |
| 완성도 기준 | 모바일 결과 화면에서 작게 보여도 캐릭터 성격이 읽혀야 함 |

### 4.2. 금지 또는 주의 요소

- 실제 정치인, 특정 정당, 특정 국가, 선거 캠프를 연상시키는 외형과 구도는 사용하지 않습니다.
- 국기, 정당 로고, 투표용지, 유세차, 의회 배지처럼 현실 정치 상징성이 강한 소품은 피합니다.
- 좌/우 진영을 직접 암시하는 색상 대비, 손동작, 슬로건, 포스터식 문구를 사용하지 않습니다.
- 특정 성별, 직업, 계층, 세대, 지역을 조롱하거나 고정관념으로 보이게 만드는 표현을 피합니다.
- 군복, 제복, 방패, 깃발 등 권위나 운동성을 암시하는 요소는 추상화해서 사용하며 실제 단체 상징처럼 보이지 않게 합니다.
- 결과 유형을 선악, 우열, 정상/비정상처럼 보이게 만드는 명암 대비와 표정 연출은 피합니다.

### 4.3. S/M 태그 표현 방식

강도 태그는 캐릭터 파일을 `-S`, `-M`으로 분리하지 않고, 동일 캐릭터 위에 UI 스타일을 덧입혀 표현합니다.

| 태그 | 표현 기준 |
|---|---|
| `-S` Strong | 결과 카드의 배지, 게이지, 포인트 컬러 대비를 강하게 적용. 캐릭터 자체는 더 공격적으로 바꾸지 않음 |
| `-M` Moderate | 배지, 게이지, 포인트 컬러를 부드럽게 적용. 캐릭터 자체는 동일하게 유지 |

### 4.4. 제작 및 검수 기준

- 16개 캐릭터는 같은 그림체, 같은 명암 밀도, 같은 디테일 수준을 유지합니다.
- 각 캐릭터의 차이는 정치적 라벨이 아니라 자세, 소품, 색 포인트, 실루엣으로 구분합니다.
- 결과 캡처 영역에서는 제목, 결과 코드, 한 줄 요약, 4축 게이지, 워터마크가 우선입니다. 캐릭터를 다시 도입하더라도 이 정보보다 시각적 우선순위가 높아지면 안 됩니다.
- 투명 배경 PNG를 우선 제공하고, 최종 배포 전 파일 용량을 모바일 로딩에 적합하게 최적화합니다.
- 로컬 MVP에서는 같은 파일명 규칙의 placeholder를 사용할 수 있으나, placeholder도 최종 캐릭터와 동일한 1:1 비율을 유지해야 합니다.

## 5. 16개 캐릭터 슬롯

아래 16개 파일은 실제 캐릭터 에셋을 도입할 때 필요한 슬롯입니다. 현재 로컬 MVP에서는 파일 슬롯 계약만 유지하고, 결과 화면은 타입 코드와 게이지 중심으로 렌더링합니다.

| type_code | 파일명                  | 성향 조합        | MVP 제작 방향              |
| --------- | -------------------- | ------------ | ---------------------- |
| `CTMO`    | `character_CTMO.png` | 공동체·전통·성과·질서 | 시스템 설계자, 정돈된 제복/도구 느낌  |
| `CTML`    | `character_CTML.png` | 공동체·전통·성과·자유 | 전통적 리더, 독립적 장인 느낌      |
| `CTEO`    | `character_CTEO.png` | 공동체·전통·평등·질서 | 보호자, 안정적 공동체 관리자 느낌    |
| `CTEL`    | `character_CTEL.png` | 공동체·전통·평등·자유 | 다정한 수호자, 마을/울타리 느낌     |
| `CPMO`    | `character_CPMO.png` | 공동체·진보·성과·질서 | 개혁적 기획자, 도시/정책 실험 느낌   |
| `CPML`    | `character_CPML.png` | 공동체·진보·성과·자유 | 네트워크형 창업가, 연대와 경쟁의 혼합  |
| `CPEO`    | `character_CPEO.png` | 공동체·진보·평등·질서 | 구조 개혁가, 공공 시스템 디자이너 느낌 |
| `CPEL`    | `character_CPEL.png` | 공동체·진보·평등·자유 | 광장 연대자, 따뜻한 군중/깃발 느낌   |
| `ITMO`    | `character_ITMO.png` | 개인·전통·성과·질서  | 원칙형 전략가, 고전적 실행가 느낌   |
| `ITML`    | `character_ITML.png` | 개인·전통·성과·자유  | 자유시장 개척자, 개척/도구/지도 느낌  |
| `ITEO`    | `character_ITEO.png` | 개인·전통·평등·질서  | 절제된 조정자, 윤리와 규칙의 균형    |
| `ITEL`    | `character_ITEL.png` | 개인·전통·평등·자유  | 조용한 독립가, 작은 공동체 느낌   |
| `IPMO`    | `character_IPMO.png` | 개인·진보·성과·질서  | 테크노크라트, 데이터/기술 장치 느낌   |
| `IPML`    | `character_IPML.png` | 개인·진보·성과·자유  | 실험적 메이커, 자유로운 해커 느낌    |
| `IPEO`    | `character_IPEO.png` | 개인·진보·평등·질서  | 제도 개혁 분석가, 균형 잡힌 미래 설계  |
| `IPEL`    | `character_IPEL.png` | 개인·진보·평등·자유  | 보헤미안 혁명가, 자유로운 움직임 느낌  |

### 5.1. 유형별 제작 해석 기준

| 축 | 일러스트 해석 기준 |
|---|---|
| `C` 공동체 | 주변 사람, 연결선, 원형 구도, 따뜻한 보조 색으로 관계성을 암시 |
| `I` 개인 | 단독 실루엣, 개인 도구, 지도/노트/장치 등 자율성을 암시 |
| `T` 전통 | 클래식한 형태, 안정적인 자세, 오래된 도구의 추상화 |
| `P` 진보 | 비대칭 구도, 실험적 소품, 데이터/빛/움직임의 추상화 |
| `M` 성과 | 정교한 도구, 설계도, 기계 장치, 날카로운 실루엣 |
| `E` 평등 | 수평적 구도, 균형 잡힌 배치, 여러 점/선의 동등한 연결 |
| `O` 질서 | 정돈된 자세, 격자/구조/프레임, 차분한 대비 |
| `L` 자유 | 움직이는 포즈, 열린 선, 바람/궤적/확산 효과 |

## 6. 공유 이미지 및 썸네일 규격

| 용도 | 파일/렌더링 규격 | 비고 |
|---|---|---|
| 인스타그램 스토리 저장 이미지 | 1080 x 1920 px, 9:16 | 결과 캡처 영역에서 생성 |
| X/트위터 공유 이미지 | 1200 x 675 px, 16:9 | 별도 OG 이미지 또는 서버/클라이언트 캡처로 생성 |
| 카카오/일반 OG 이미지 | 1200 x 630 px | `og_default.png` 및 결과별 OG 확장 가능 |
| 랜딩 메인 비주얼 | 선택 | 현재 로컬 MVP는 별도 랜딩 비주얼 없이 텍스트 중심 시작 화면 |

### 6.1. OG 기본 이미지 파일 계약

| 항목 | 규격 |
|---|---|
| 저장 위치 | `static/assets/og/` |
| 기본 파일명 | `og_default.png` |
| 권장 크기 | 1200 x 630 px |
| 용도 | 랜딩, 결과 페이지 기본 OG/Twitter Card 이미지 |
| 필수 포함 | `Poldigm` 로고 또는 워드마크, 테스트 핵심 카피, 과도하게 민감하지 않은 비주얼 |

로컬 MVP에서는 실제 이미지가 없어도 되지만, 실제 MVP 배포 전에는 `static/assets/og/og_default.png`를 반드시 준비합니다. 결과별 OG 이미지는 공유 성과를 확인한 뒤 후속 확장으로 검토합니다.

## 7. UI 연동 계약

- 결과 매핑의 `character_img` 값은 파일명만 저장합니다. 예: `character_CTMO.png`.
- 캐릭터를 다시 도입할 경우 프론트엔드는 `static/assets/characters/{character_img}` 경로로 이미지를 참조합니다.
- 이미지 로딩 실패 시 타입 코드 기반 대체 마크 또는 캐릭터 없는 결과 카드로 fallback합니다.
- `-S`/`-M` 결과는 캐릭터가 아니라 결과 코드 pill, 게이지, 문구 톤에서 구분합니다.
- 캐릭터 이미지의 `alt` 텍스트는 `{title} 캐릭터 일러스트` 형식을 사용합니다.
- placeholder는 같은 타입 코드와 동일한 표시 비율을 유지해야 하며, 최종 이미지 교체 시 레이아웃이 바뀌지 않아야 합니다.
- 파일 용량 최적화는 PNG 투명 배경을 우선하되, 랜딩 비주얼처럼 투명이 필요 없는 경우 WebP를 사용할 수 있습니다.

## 8. MVP 완료 기준

- 16개 `character_{type_code}.png` 슬롯 계약이 정의되어 있어야 합니다.
- 최종 일러스트가 없더라도 결과 화면은 타입 코드, 결과명, 한 줄 설명, 4축 게이지로 완성되어야 합니다.
- 캐릭터를 다시 도입할 경우 공통 스타일, 금지 요소, S/M 표현 방식이 제작 기준에 반영되어야 합니다.
- `static/assets/og/og_default.png`가 실제 MVP 배포 전 준비되어 있어야 합니다.
- 결과 화면 캡처 영역에서 제목, 결과 코드, 한 줄 설명, 4축 게이지, 워터마크가 한 화면 안에 들어와야 합니다.
- 공유 이미지 생성 시 텍스트가 잘리지 않고 `Poldigm.com` 워터마크가 포함되어야 합니다.

## 9. 현재 구현 반영

현재 로컬 MVP에는 실제 캐릭터 PNG가 포함되어 있지 않습니다. 결과 화면의 기본 요약 카드도 캐릭터를 표시하지 않고, 결과명, 결과 코드 pill, 한 줄 설명, 4축 게이지 중심으로 렌더링합니다. `src/lib/components/result/CharacterImage.svelte`는 향후 캐릭터 도입 또는 fallback 실험을 위해 보존된 컴포넌트입니다.

| 항목 | 현재 상태 |
|---|---|
| 캐릭터 파일명 계약 | `character_{type_code}.png` 유지 |
| 실제 PNG | 미적용 |
| 로컬 결과 화면 | 캐릭터 없이 타입 코드 pill + 결과명 + 4축 게이지 표시 |
| 교체 방식 | 실제 MVP에서 `static/assets/characters/character_{type_code}.png` 추가 후 동일 UI 계약 유지 |
| 검증 | Playwright 결과 화면 캡처, `npm run check`, `npm run build` 통과 |

실제 일러스트 제작 시에도 `character_img` 파일명 계약은 바꾸지 않습니다. 단, 캐릭터가 결과 화면의 핵심 정보보다 큰 시각적 비중을 차지하지 않도록 화면별 표시 크기는 재검수합니다.
